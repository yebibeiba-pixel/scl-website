# دليل البدء السريع - ربط الموقع بالنطاق scl-communication.com

## 🎯 الهدف
ربط موقع SCL Communication بالنطاق **scl-communication.com** وجعله متاحاً للجمهور.

---

## ⚡ الخطوات السريعة (للمبتدئين)

### الخطوة 1️⃣: إعداد DNS (5 دقائق)

1. **سجّل الدخول إلى لوحة تحكم النطاق** (حيث اشتريت النطاق)
   - مثال: GoDaddy, Namecheap, Google Domains

2. **ابحث عن قسم DNS أو DNS Management**

3. **أضف السجلات التالية:**

   إذا كنت ستستخدم **Railway** (الطريقة الأسهل):
   ```
   انتظر حتى الخطوة 2، سيعطيك Railway السجلات المطلوبة
   ```

   إذا كان لديك **خادم VPS** بعنوان IP:
   ```
   Type: A
   Name: @
   Value: [عنوان IP الخاص بخادمك]
   TTL: 3600

   Type: A
   Name: www
   Value: [عنوان IP الخاص بخادمك]
   TTL: 3600
   ```

4. **احفظ التغييرات** وانتظر 10-30 دقيقة لانتشار DNS

---

### الخطوة 2️⃣: اختر طريقة النشر

#### 🚀 الطريقة الأولى: Railway (موصى بها للمبتدئين)

**المميزات:**
- ✅ سهلة جداً
- ✅ لا تحتاج خبرة تقنية
- ✅ قاعدة بيانات مجانية
- ✅ SSL تلقائي
- ✅ تكلفة منخفضة (5-10$ شهرياً)

**الخطوات:**

1. **إنشاء حساب على Railway:**
   - اذهب إلى https://railway.app
   - سجل الدخول باستخدام GitHub

2. **رفع المشروع إلى GitHub:**
   ```bash
   # على جهازك المحلي
   cd moov-mauritel-fiber
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/moov-mauritel-fiber.git
   git push -u origin main
   ```

3. **إنشاء مشروع جديد في Railway:**
   - اضغط على "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع الذي رفعته

4. **إضافة قاعدة بيانات MySQL:**
   - في لوحة Railway، اضغط على "+ New"
   - اختر "Database" → "MySQL"
   - انسخ رابط الاتصال `DATABASE_URL`

5. **إعداد متغيرات البيئة:**
   - اذهب إلى "Variables"
   - أضف المتغيرات التالية:
   ```
   DATABASE_URL=[الرابط من الخطوة السابقة]
   JWT_SECRET=أنشئ_مفتاح_سري_قوي_هنا
   PORT=3001
   NODE_ENV=production
   VITE_APP_TITLE=SCL Communication - Moov Mauritel
   ```

6. **ربط النطاق:**
   - اذهب إلى "Settings" → "Domains"
   - اضغط على "Custom Domain"
   - أدخل `scl-communication.com`
   - انسخ سجلات DNS المعروضة
   - أضفها في لوحة تحكم النطاق (الخطوة 1)

7. **انتظر النشر:**
   - Railway سينشر الموقع تلقائياً
   - بعد 5-10 دقائق، افتح https://scl-communication.com

---

#### 💻 الطريقة الثانية: VPS (للمحترفين)

**المميزات:**
- ✅ تحكم كامل
- ✅ أداء أفضل
- ✅ تكلفة ثابتة

**المتطلبات:**
- خادم VPS (DigitalOcean, Linode, AWS, إلخ)
- معرفة أساسية بـ Linux و SSH

**الخطوات المختصرة:**

```bash
# 1. الاتصال بالخادم
ssh root@your-server-ip

# 2. تثبيت المتطلبات
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt update && sudo apt install -y nodejs mysql-server nginx certbot python3-certbot-nginx
npm install -g pnpm pm2

# 3. إعداد قاعدة البيانات
sudo mysql
CREATE DATABASE moov_mauritel_fiber;
CREATE USER 'moov_user'@'localhost' IDENTIFIED BY 'كلمة_سر_قوية';
GRANT ALL PRIVILEGES ON moov_mauritel_fiber.* TO 'moov_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 4. رفع المشروع
mkdir -p /var/www/scl-communication
cd /var/www/scl-communication
# ارفع الملف المضغوط moov-mauritel-fiber-ready-for-deployment.tar.gz
tar -xzf moov-mauritel-fiber-ready-for-deployment.tar.gz
cd moov-mauritel-fiber

# 5. تثبيت وبناء
pnpm install
# عدّل ملف .env بالبيانات الصحيحة
pnpm build

# 6. تشغيل بـ PM2
pm2 start dist/index.js --name scl-communication
pm2 save
pm2 startup

# 7. إعداد Nginx
sudo nano /etc/nginx/sites-available/scl-communication.com
# انسخ التكوين من DEPLOYMENT_GUIDE.md
sudo ln -s /etc/nginx/sites-available/scl-communication.com /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 8. تفعيل SSL
sudo certbot --nginx -d scl-communication.com -d www.scl-communication.com
```

**للتفاصيل الكاملة، راجع:** [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

---

## 🔍 التحقق من النشر

بعد إكمال الخطوات:

1. **افتح المتصفح:**
   ```
   https://scl-communication.com
   ```

2. **تأكد من:**
   - ✅ الموقع يظهر بشكل صحيح
   - ✅ القفل الأخضر (SSL) يظهر
   - ✅ جميع الصور تظهر
   - ✅ نموذج التسجيل يعمل

3. **اختبر على الهاتف:**
   - افتح الموقع من هاتفك المحمول
   - تأكد من أن التصميم متجاوب

---

## ❓ الأسئلة الشائعة

### س: كم من الوقت يستغرق انتشار DNS؟
**ج:** عادة 10-30 دقيقة، لكن قد يستغرق حتى 48 ساعة في بعض الحالات.

### س: ما هي التكلفة الشهرية؟
**ج:** 
- Railway: 5-10$ شهرياً
- VPS (DigitalOcean): 6-12$ شهرياً
- VPS (Linode): 5-10$ شهرياً

### س: هل أحتاج إلى معرفة برمجية؟
**ج:** 
- Railway: لا، سهل جداً
- VPS: نعم، تحتاج معرفة أساسية بـ Linux

### س: كيف أضيف مستخدم إداري؟
**ج:** راجع قسم "إضافة مستخدم إداري" في [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

### س: كيف أحدّث الموقع بعد النشر؟
**ج:**
- **Railway:** ارفع التحديثات إلى GitHub، سيتم النشر تلقائياً
- **VPS:** راجع قسم "تحديث المشروع" في [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. **راجع الدليل الشامل:** [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)
2. **تحقق من السجلات:**
   - Railway: في لوحة التحكم → "Deployments" → "Logs"
   - VPS: `pm2 logs scl-communication`
3. **تواصل معنا:**
   - البريد: info@scl-communication.mr
   - الهاتف: 0022244292222

---

## ✅ قائمة التحقق السريعة

قبل البدء، تأكد من أن لديك:

- [ ] النطاق scl-communication.com محجوز
- [ ] وصول إلى لوحة تحكم DNS للنطاق
- [ ] ملف المشروع `moov-mauritel-fiber-ready-for-deployment.tar.gz`
- [ ] حساب على Railway أو خادم VPS
- [ ] بريد إلكتروني للتسجيل في الخدمات

---

**حظاً موفقاً! 🚀**

إذا اتبعت الخطوات بعناية، سيكون موقعك متاحاً على https://scl-communication.com خلال ساعة واحدة.

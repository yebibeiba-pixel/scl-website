# مشروع موقع SCL Communication - Moov Mauritel Fiber

## 📋 نظرة عامة
موقع تسويقي لخدمات الإنترنت عبر الألياف البصرية من شركة SCL Communication (وكيل معتمد لمووف موريتل في موريتانيا).

## 🚀 التقنيات المستخدمة
- **Frontend**: React 19 + TypeScript + Vite
- **Backend**: Express 4 + tRPC 11
- **Database**: MySQL/TiDB (Drizzle ORM)
- **Styling**: Tailwind CSS 4 + shadcn/ui
- **Maps**: Leaflet + OpenStreetMap
- **Authentication**: Manus OAuth

## 📦 التثبيت والتشغيل

### المتطلبات
- Node.js 22.13.0+
- pnpm

### خطوات التشغيل
```bash
# 1. فك ضغط الملفات
tar -xzf moov-mauritel-fiber-export.tar.gz
cd moov-mauritel-fiber

# 2. تثبيت المكتبات
pnpm install

# 3. إعداد قاعدة البيانات
pnpm db:push

# 4. تشغيل السيرفر
pnpm dev
```

الموقع سيعمل على: http://localhost:3000

## 🗂️ هيكل المشروع

```
moov-mauritel-fiber/
├── client/                 # Frontend (React)
│   ├── src/
│   │   ├── pages/         # الصفحات الرئيسية
│   │   │   ├── Home.tsx   # الصفحة الرئيسية
│   │   │   ├── Admin.tsx  # لوحة التحكم
│   │   │   ├── Contract.tsx # صفحة العقد
│   │   │   └── TrackOrder.tsx # تتبع الطلبات
│   │   ├── components/    # المكونات المشتركة
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── LanguageSwitcher.tsx
│   │   │   └── CoverageMap.tsx
│   │   ├── translations.ts # الترجمات (عربي/فرنسي)
│   │   └── utils/
│   │       └── coverageAreas.ts # مناطق التغطية
│   └── public/            # الملفات الثابتة
│       ├── scl-logo-small.webp
│       └── moov-mauritel-logo-small.webp
├── server/                # Backend (Express + tRPC)
│   ├── routers.ts        # API Routes
│   └── db.ts             # Database queries
├── drizzle/              # Database schema
│   └── schema.ts
└── shared/               # الملفات المشتركة
```

## 🌟 الميزات الرئيسية

### 1. نظام ثنائي اللغة
- دعم كامل للعربية والفرنسية
- تبديل تلقائي بين RTL/LTR
- زر تبديل اللغة مع أعلام SVG

### 2. نظام التسجيل
- نموذج تسجيل مبسط
- التحقق التلقائي من التغطية عبر الموقع الجغرافي
- إشعارات تلقائية للمسؤول

### 3. لوحة التحكم (Admin Dashboard)
- إدارة التسجيلات
- خريطة تفاعلية لعرض مواقع العملاء
- نظام إدارة المستخدمين مع الصلاحيات (Admin, Manager, Agent, Viewer)
- تصدير التقارير إلى CSV

### 4. العقود الإلكترونية
- عقود احترافية مع جميع التفاصيل
- لوحة توقيع إلكتروني (ماوس/لمس)
- حفظ التوقيعات في قاعدة البيانات

### 5. الخرائط التفاعلية
- خريطة Leaflet مع OpenStreetMap
- عرض مواقع العملاء مع مناطق التغطية الملونة
- تكامل مع Google Maps وخريطة موريتل الرسمية

## 🎨 التصميم
- ألوان الشركة: أزرق (#0066CC) وبرتقالي (#FF6B00)
- متجاوب بالكامل (موبايل، تابلت، ديسكتوب)
- قائمة همبرغر للهواتف
- تحسينات أداء شاملة (98% تقليل حجم الصور)

## 📊 قاعدة البيانات

### الجداول الرئيسية:
1. **users** - المستخدمين (OAuth)
2. **registrations** - تسجيلات العملاء
3. **staffUsers** - المستخدمين الإداريين

## 🔐 الصلاحيات (Roles)
- **Admin**: صلاحيات كاملة
- **Manager**: عرض، تعديل، تصدير
- **Agent**: عرض، تعديل
- **Viewer**: عرض فقط

## 📱 معلومات الاتصال
- الهاتف: 0022244292222
- البريد: info@scl-communication.mr
- الموقع: www.scl-communication.mr

## 📦 الباقات المتوفرة
1. **100 ميغابايت**: 1500 أوقية/شهر
2. **200 ميغابايت**: 2500 أوقية/شهر
3. **500 ميغابايت**: 5000 أوقية/شهر

**ملاحظة**: تكلفة المودم 1000 أوقية (دفعة واحدة فقط)

## 🔧 ملاحظات للتطوير

### متغيرات البيئة المطلوبة:
```env
DATABASE_URL=mysql://...
JWT_SECRET=...
VITE_APP_ID=...
OAUTH_SERVER_URL=...
VITE_OAUTH_PORTAL_URL=...
```

### الأوامر المهمة:
```bash
pnpm dev          # تشغيل السيرفر
pnpm build        # بناء المشروع
pnpm db:push      # تحديث قاعدة البيانات
pnpm db:studio    # فتح واجهة قاعدة البيانات
```

## 📝 التحسينات المستقبلية (اختياري)
- [ ] نسخة فرنسية كاملة لصفحات Admin و Contract
- [ ] تكامل WhatsApp Business
- [ ] بوابة الدفع الإلكتروني
- [ ] ربط النطاق المخصص

## 📞 الدعم
للأسئلة أو المساعدة، يمكنك التواصل مع المطور الأصلي.

---
تم التطوير بواسطة Manus AI
آخر تحديث: نوفمبر 2025
